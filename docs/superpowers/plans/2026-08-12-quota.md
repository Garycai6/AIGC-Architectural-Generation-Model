# 免费额度限制(Quota)实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 每个匿名访客每天可免费生成 N 次(`Settings.max_free_quota`),超限返回 429,前端展示剩余次数与超限提示。

**Architecture:** 新增纯逻辑类 `QuotaService`(内存 dict 按 `visitor_id → {date_str: count}` 计数,新的一天自然新 key 即重置);`create_app` 把实例挂到 `app.state.quota_service`;`generate` 路由开头读 `X-Visitor-Id` 请求头 → `consume` → 超限抛 `HTTPException(429)`。`GenerationResponse` 增加 `remaining_quota` 字段供前端展示。前端首次加载用 `crypto.randomUUID()` 生成访客 ID 存 localStorage,请求带头。

**Tech Stack:** Python 3.11 / FastAPI / pydantic / pytest + ruff(E/F/I/UP/B, line-length=100);React 18 + TypeScript + Vite(前端无测试设施,验证 = `tsc && vite build` + 浏览器)。

## Global Constraints

- ruff:`line-length = 100`,`select = ["E", "F", "I", "UP", "B"]`,check + format 双绿
- commit 前缀:`feat:` / `fix:` / `docs:`;消息用中文
- 后端测试显式传 `Settings`(避免 .env / 环境变量),`_make_app` 已传 `max_free_quota=5`
- **向后兼容**:无 `X-Visitor-Id` 头的请求放行,不消费额度,`remaining_quota` 报满额
- 429 用 FastAPI 标准错误体(`{"detail": "今日免费额度已用完"}`),**不含** `remaining_quota`;前端对 200 读 `remaining_quota`,对 429 读本地 i18n 文案
- 不改:SimulatorGenerator、ApiGenerator、replicate 分支、现有控件行为
- 设计文档 `docs/superpowers/specs/2026-08-12-quota-design.md` 为唯一需求来源,若实现需偏离须先经用户批准

---

### Task 1: QuotaService 纯逻辑类 + 单元测试

**Files:**
- Create: `backend/app/core/quota.py`
- Test: `tests/test_quota.py`

**Interfaces:**
- Consumes: 无(独立纯逻辑类)
- Produces:
  - `class QuotaService` 位于 `backend.app.core.quota`
  - `QuotaService.__init__(self, max_free_quota: int)`
  - `QuotaService.consume(self, visitor_id: str, date_str: str) -> int` — 消费一次,返回**本次消费后的剩余次数**(≥0);已达上限返回 0 且不累加
  - `QuotaService.remaining(self, visitor_id: str, date_str: str) -> int` — 只读查询剩余次数

- [ ] **Step 1: 写失败测试**

创建 `tests/test_quota.py`:

```python
from backend.app.core.quota import QuotaService


def test_consume_within_limit():
    q = QuotaService(max_free_quota=3)
    assert q.consume("v1", "2026-08-12") == 2
    assert q.consume("v1", "2026-08-12") == 1
    assert q.consume("v1", "2026-08-12") == 0


def test_consume_over_limit_returns_zero_no_increment():
    q = QuotaService(max_free_quota=1)
    assert q.consume("v1", "2026-08-12") == 0
    assert q.consume("v1", "2026-08-12") == 0  # 超限后不再累加,保持 0
    assert q.remaining("v1", "2026-08-12") == 0


def test_resets_on_new_date():
    q = QuotaService(max_free_quota=2)
    q.consume("v1", "2026-08-12")
    q.consume("v1", "2026-08-12")
    assert q.consume("v1", "2026-08-12") == 0  # 当天额度耗尽
    assert q.consume("v1", "2026-08-13") == 1  # 新的一天额度刷新 → 2-1=1


def test_isolated_per_visitor():
    q = QuotaService(max_free_quota=2)
    q.consume("v1", "2026-08-12")
    q.consume("v1", "2026-08-12")
    assert q.consume("v1", "2026-08-12") == 0  # v1 耗尽
    assert q.consume("v2", "2026-08-12") == 1  # v2 独立计数 → 2-1=1


def test_remaining_read_only():
    q = QuotaService(max_free_quota=5)
    assert q.remaining("v1", "2026-08-12") == 5
    q.consume("v1", "2026-08-12")
    assert q.remaining("v1", "2026-08-12") == 4
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_quota.py -v`
Expected: FAIL(ModuleNotFoundError: `backend.app.core.quota` 不存在)

- [ ] **Step 3: 写最小实现**

创建 `backend/app/core/quota.py`:

```python
import threading


class QuotaService:
    """匿名访客每日免费额度计数(内存存储,重启清零)。

    结构:`{visitor_id: {date_str: used_count}}`。新的一天出现新 date_str
    即自然重置,无需定时器。
    """

    def __init__(self, max_free_quota: int):
        self._max = max_free_quota
        self._counts: dict[str, dict[str, int]] = {}
        self._lock = threading.Lock()

    def consume(self, visitor_id: str, date_str: str) -> int:
        """消费一次,返回本次消费后的剩余次数(≥0)。已达上限返回 0 且不累加。"""
        with self._lock:
            per_day = self._counts.setdefault(visitor_id, {})
            used = per_day.get(date_str, 0)
            if used >= self._max:
                return 0
            per_day[date_str] = used + 1
            return self._max - (used + 1)

    def remaining(self, visitor_id: str, date_str: str) -> int:
        with self._lock:
            used = self._counts.get(visitor_id, {}).get(date_str, 0)
            return max(self._max - used, 0)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_quota.py -v`
Expected: 5 个 PASS

- [ ] **Step 5: ruff + 提交**

Run: `uv run ruff check backend/app/core/quota.py tests/test_quota.py && uv run ruff format --check backend/app/core/quota.py tests/test_quota.py`
Expected: 无告警

```bash
git add backend/app/core/quota.py tests/test_quota.py
git commit -m "feat: 新增 QuotaService 匿名访客每日额度计数(纯逻辑类 + 单元测试)"
```

---

### Task 2: 接线 create_app + generate 路由 + 响应体 + 路由测试

**Files:**
- Modify: `backend/app/main.py:14`(create_app 挂载 quota_service)
- Modify: `backend/app/api/generate.py:17-54`(路由读头 + 消费额度 + 429)
- Modify: `backend/app/schemas/generate.py:13-16`(GenerationResponse 加 remaining_quota)
- Test: `tests/test_api.py`(末尾追加路由级额度测试)

**Interfaces:**
- Consumes:
  - `QuotaService`(Task 1,`backend.app.core.quota`)
  - `Settings.max_free_quota`(现有字段,默认 5)
- Produces:
  - `request.app.state.quota_service: QuotaService`
  - `GenerationResponse.remaining_quota: int = 0`
  - 无 `X-Visitor-Id` 头 → 放行,`remaining_quota = settings.max_free_quota`
  - 有头且超限 → `HTTPException(status_code=429, detail="今日免费额度已用完")`

- [ ] **Step 1: 写失败测试(追加到 tests/test_api.py 末尾)**

```python
def _quota_payload():
    return {
        "params": {
            "style": "modern",
            "floors": 3,
            "width_m": 10.0,
            "depth_m": 8.0,
            "materials": ["glass"],
            "roof": "flat",
            "environment": "suburb",
        },
        "lang": "zh",
    }


def test_generate_quota_exhausted_after_max(tmp_path):
    from backend.app.core.config import Settings

    settings = Settings(
        deepseek_api_key="",
        deepseek_base_url="https://api.deepseek.com",
        image_provider="simulator",
        max_free_quota=3,
        cache_dir=str(tmp_path),
    )
    client = TestClient(create_app(settings))
    headers = {"X-Visitor-Id": "visitor-1"}
    for i in range(3):
        resp = client.post("/api/v1/generate", json=_quota_payload(), headers=headers)
        assert resp.status_code == 200
        assert resp.json()["remaining_quota"] == 2 - i  # 2, 1, 0
    resp = client.post("/api/v1/generate", json=_quota_payload(), headers=headers)
    assert resp.status_code == 429
    assert resp.json() == {"detail": "今日免费额度已用完"}


def test_generate_quota_isolated_per_visitor(tmp_path):
    from backend.app.core.config import Settings

    settings = Settings(
        deepseek_api_key="",
        deepseek_base_url="https://api.deepseek.com",
        image_provider="simulator",
        max_free_quota=1,
        cache_dir=str(tmp_path),
    )
    client = TestClient(create_app(settings))
    # visitor-a 用满 1 次
    resp = client.post(
        "/api/v1/generate", json=_quota_payload(), headers={"X-Visitor-Id": "visitor-a"}
    )
    assert resp.status_code == 200
    # visitor-a 再次请求 → 429
    resp = client.post(
        "/api/v1/generate", json=_quota_payload(), headers={"X-Visitor-Id": "visitor-a"}
    )
    assert resp.status_code == 429
    # visitor-b 独立计数 → 200
    resp = client.post(
        "/api/v1/generate", json=_quota_payload(), headers={"X-Visitor-Id": "visitor-b"}
    )
    assert resp.status_code == 200
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_api.py::test_generate_quota_exhausted_after_max tests/test_api.py::test_generate_quota_isolated_per_visitor -v`
Expected: 2 个 FAIL(无头也放行,永不 429;`remaining_quota` 字段不存在)

- [ ] **Step 3: 实现**

**修改 `backend/app/schemas/generate.py`** — `GenerationResponse` 加字段:

```python
class GenerationResponse(BaseModel):
    scheme_id: str
    description: str
    images: list[str] = []
    remaining_quota: int = 0  # 本次消费后的剩余次数(无头请求报满额)
```

**修改 `backend/app/main.py`** — `create_app` 挂载 quota_service:

```python
from backend.app.core.config import Settings, get_settings
from backend.app.core.quota import QuotaService
```

在 `app.state.settings = settings or get_settings()` 之后加一行:

```python
    app.state.quota_service = QuotaService(app.state.settings.max_free_quota)
```

**修改 `backend/app/api/generate.py`** — import 区加:

```python
from datetime import date
```

并在现有 `from backend.app.core.config import Settings` 后加:

```python
from backend.app.core.quota import QuotaService
```

`generate` 路由开头(settings 读取之后、`scheme_id` 之前)插入:

```python
    quota_service: QuotaService = request.app.state.quota_service
    remaining_quota = settings.max_free_quota  # 无头请求默认报满额(向后兼容)
    visitor_id = request.headers.get("X-Visitor-Id")
    if visitor_id:
        remaining_quota = quota_service.consume(visitor_id, date.today().isoformat())
        if remaining_quota == 0:
            raise HTTPException(status_code=429, detail="今日免费额度已用完")
```

`return GenerationResponse(...)` 加 `remaining_quota`:

```python
    return GenerationResponse(
        scheme_id=scheme_id,
        description=description,
        images=[img.url for img in artifact.images],
        remaining_quota=remaining_quota,
    )
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_api.py -v`
Expected: 新增 2 个 PASS,原有测试(含 `test_generate_skeleton`、mock 相关)仍 PASS(它们不发 `X-Visitor-Id`,走放行分支)

- [ ] **Step 5: ruff + 提交**

Run: `uv run ruff check backend/app/main.py backend/app/api/generate.py backend/app/schemas/generate.py tests/test_api.py && uv run ruff format --check backend/app/main.py backend/app/api/generate.py backend/app/schemas/generate.py tests/test_api.py`
Expected: 双绿

```bash
git add backend/app/main.py backend/app/api/generate.py backend/app/schemas/generate.py tests/test_api.py
git commit -m "feat: /generate 接入每日额度检查(读 X-Visitor-Id,超限返回 429)"
```

---

### Task 3: 前端访客 ID 生成与请求头携带

**Files:**
- Modify: `frontend/src/api/client.ts`

**Interfaces:**
- Consumes: 无(纯前端)
- Produces:
  - `export class QuotaExhaustedError extends Error` — 429 专用错误类
  - `export function getVisitorId(): string` — 读 `localStorage["archgen_visitor_id"]`,无则 `crypto.randomUUID()` 生成并持久化
  - `GenerateResponse` 增加 `remaining_quota: number`
  - `generateScheme` 请求头加 `X-Visitor-Id`;收到 429 抛 `QuotaExhaustedError`

- [ ] **Step 1: 实现**

修改 `frontend/src/api/client.ts`,把 `GenerateResponse` 加字段,并新增错误类、`getVisitorId`,改 `generateScheme`:

```ts
export class QuotaExhaustedError extends Error {}

const VISITOR_ID_KEY = "archgen_visitor_id";

export function getVisitorId(): string {
  let id = localStorage.getItem(VISITOR_ID_KEY);
  if (!id) {
    id = crypto.randomUUID();
    localStorage.setItem(VISITOR_ID_KEY, id);
  }
  return id;
}

export interface GenerateResponse {
  scheme_id: string;
  description: string;
  images: string[];
  remaining_quota: number;
}

export async function generateScheme(params: BuildingParams, lang = "zh"): Promise<GenerateResponse> {
  const resp = await fetch("/api/v1/generate", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "X-Visitor-Id": getVisitorId(),
    },
    body: JSON.stringify({ params, lang }),
  });
  if (resp.status === 429) throw new QuotaExhaustedError();
  if (!resp.ok) throw new Error(`HTTP ${resp.status}`);
  return resp.json();
}
```

- [ ] **Step 2: build 验证**

Run: `cd frontend && npm run build`
Expected: `tsc && vite build` 通过,无 TS 类型错误

- [ ] **Step 3: 提交**

```bash
git add frontend/src/api/client.ts
git commit -m "feat: 前端访客 ID 生成与请求头携带(429 抛 QuotaExhaustedError)"
```

---

### Task 4: 前端剩余额度展示 + 429 提示(i18n)

**Files:**
- Modify: `frontend/src/components/ParamForm/ParamForm.tsx`
- Modify: `frontend/src/i18n/zh.json`
- Modify: `frontend/src/i18n/en.json`

**Interfaces:**
- Consumes:
  - `QuotaExhaustedError`、`getVisitorId`(Task 3)
  - `messages.quota_remaining` / `messages.quota_exhausted`(i18n,本任务新增)
- Produces: 结果区显示「今日剩余生成次数:N」;429 时错误区显示「今日免费额度已用完」

- [ ] **Step 1: i18n 加翻译**

修改 `frontend/src/i18n/zh.json`,在 `"generate"` 之后加:

```json
  "quota_remaining": "今日剩余生成次数:", "quota_exhausted": "今日免费额度已用完",
```

修改 `frontend/src/i18n/en.json`,同样位置加:

```json
  "quota_remaining": "Remaining free quota today: ", "quota_exhausted": "Free quota for today is used up",
```

- [ ] **Step 2: ParamForm 展示剩余额度 + 区分 429**

修改 `frontend/src/components/ParamForm/ParamForm.tsx`:

import 区把 `import { generateScheme } from "../../api/client";` 改为:

```tsx
import { generateScheme, QuotaExhaustedError } from "../../api/client";
```

state 加一个:

```tsx
  const [remainingQuota, setRemainingQuota] = useState<number | null>(null);
```

`onSubmit` 的 try 成功分支里,`setImages` 之前加:

```tsx
      setRemainingQuota(res.remaining_quota);
```

`catch` 改为区分 429:

```tsx
    } catch (err) {
      setError(err instanceof QuotaExhaustedError ? messages.quota_exhausted : messages.error);
    } finally {
```

`{error && ...}` 之后、结果图 `<div>` 之前加展示:

```tsx
      {remainingQuota !== null && (
        <p style={{ marginTop: "0.5rem" }}>{messages.quota_remaining}{remainingQuota}</p>
      )}
```

- [ ] **Step 3: build 验证**

Run: `cd frontend && npm run build`
Expected: `tsc && vite build` 通过,无 TS 类型错误

- [ ] **Step 4: 提交**

```bash
git add frontend/src/components/ParamForm/ParamForm.tsx frontend/src/i18n/zh.json frontend/src/i18n/en.json
git commit -m "feat: 前端剩余额度展示与 429 提示(i18n 双语)"
```

---

### Task 5: 全量回归 + smoke-test 记录

**Files:**
- Modify: `docs/gallery/smoke-test.md`

**Interfaces:**
- Consumes: Task 1-4 全部改动
- Produces: 冒烟验证记录(额度限制生效证据)

- [ ] **Step 1: 全量测试 + ruff 双绿**

Run: `uv run pytest -q`
Expected: 95(原有)+ 5(Task1)+ 2(Task2)= **102 passed + 1 skipped**(`test_training_skip.py` 本机无 torch 跳过)
Run: `uv run ruff check . && uv run ruff format --check .`
Expected: 双绿

- [ ] **Step 2: 浏览器验证**

1. 启动后端:主工作区根目录 `uv run archgen-api`
2. 启动前端:`cd frontend && npm run dev`(http://localhost:5173)
3. 打开页面 → 首次加载后 Network 面板确认请求带 `X-Visitor-Id`(UUID);生成一次 → 结果区显示「今日剩余生成次数:4」(默认 max=5)
4. 连续生成至第 6 次 → 错误区显示「今日免费额度已用完」,Network 确认第 6 次请求返回 429
5. 切换 EN → 显示 "Remaining free quota today: N" / "Free quota for today is used up"
6. localStorage 确认 `archgen_visitor_id` 已持久化;刷新页面再生成 → 次数继续累减(同一访客)

- [ ] **Step 3: 追加 smoke-test 记录**

在 `docs/gallery/smoke-test.md` 末尾追加:

```markdown
# 免费额度限制(Quota)验证记录 (2026-08-12)

- QuotaService:匿名访客每日额度计数(内存 dict,新日期自然重置),consume 返回剩余次数,超限不累加
- /generate 读 X-Visitor-Id 头;无头请求放行(向后兼容);超限返回 429 标准错误体
- GenerationResponse 新增 remaining_quota;前端 localStorage 持 visitor_id(UUID)+ 请求带头
- 前端展示「今日剩余生成次数:N」;429 显示「今日免费额度已用完」;EN 切换正常
- 浏览器验证:同一访客第 6 次生成 → 429;刷新页面次数继续累减;不同访客独立计数
- 全量回归:102 passed + 1 skipped;ruff check + format 双绿
- 遗留:付费解锁、持久化、登录用户维度计数均留待后续(本期仅免费额度限制)
```

- [ ] **Step 4: 提交**

```bash
git add docs/gallery/smoke-test.md
git commit -m "docs: 免费额度限制验证记录(全量回归 + 浏览器确认)"
```

---

## 验收标准(对照设计文档)

| 设计要求 | 对应任务 | 验证 |
|---|---|---|
| `QuotaService.consume` 限额内返回剩余>0、超限返回 0 | Task 1 | `test_consume_within_limit` / `test_consume_over_limit_returns_zero_no_increment` |
| 每日重置(新日期自然新 key) | Task 1 | `test_resets_on_new_date` |
| 不同访客隔离 | Task 1/2 | `test_isolated_per_visitor`(单元)+ `test_generate_quota_isolated_per_visitor`(路由) |
| `create_app` 挂 `app.state.quota_service` | Task 2 | 路由级测试用同一 app 实例累加计数 |
| 无头请求放行、`remaining_quota` 报满额 | Task 2 | 现有测试仍绿(不发头)|
| 超限抛 429 标准错误体 | Task 2 | `test_generate_quota_exhausted_after_max` 断言 `{"detail": ...}` |
| `GenerationResponse.remaining_quota` | Task 2 | 路由测试断言 200 递减 2/1/0 |
| 前端 `getVisitorId` + `X-Visitor-Id` 头 | Task 3 | `npm run build` + 浏览器 Network |
| 前端展示剩余次数 + 429 提示(i18n) | Task 4 | `npm run build` + 浏览器中英切换 |
| 全量回归 102 passed + 1 skipped | Task 5 | `uv run pytest -q` |
| 不改 SimulatorGenerator/ApiGenerator/replicate 分支 | 全计划 | diff 检查 |
