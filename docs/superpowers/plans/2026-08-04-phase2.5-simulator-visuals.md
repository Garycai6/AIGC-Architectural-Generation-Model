# 阶段 2.5:模拟器视觉差异化 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 `roof` 与 `style` 参数真正影响模拟器渲染输出——写实屋顶(三种形态)+ 四风格差异(窗型/檐口/配色),提升护城河价值与视觉质量。

**Architecture:** 数据模型驱动。升级 `FacadeSpec` 携带全部视觉信息(`RoofGeometry`/`CorniceSpec`/升级版 `WindowRect`/`style`),renderer 零风格分支。风格差异用**策略子类**:`StyleConfig` 抽象基类 + `Modern/Neoclassic/European/NordicStyle` 四个子类,`STYLE_REGISTRY` 仅做字符串→子类工厂映射。renderer 重写顶面/侧壁投影按 `RoofGeometry` 画写实屋顶。

**Tech Stack:** Python 3.11、Pillow、pydantic v2、pytest、ruff。

## Global Constraints

- Python >= 3.11,包管理用 `uv`,不用 pip 直接装
- ruff lint 与 format 必须通过;pytest 全绿才算任务完成
- 提交信息用 `feat:` / `fix:` / `docs:` 前缀
- 确定性渲染:全部由 `BuildingParams` 唯一决定,无随机
- 本阶段不改前端、API、Generator 协议;不接真 API;不改 floorplan 渲染
- 兼容已有接口:`FacadeSpec` 保留 `roof` 字段(测试 `test_roof_style_variation` 断言 `flat.roof == "flat"`),`build_facade_spec(params, width_px, height_px)` 签名不变
- ruff 已排除 `docs/`,文档内代码块不受格式约束

---

### Task 1: FacadeSpec 数据模型升级(StyleConfig 基类 + RoofGeometry + CorniceSpec)

**Files:**
- Modify: `generation/generators/simulator/facade.py`
- Modify: `tests/test_facade.py`

**Interfaces:**
- Consumes: `BuildingParams`(已有)、`MaterialPalette`/`WindowRect`/`MATERIAL_COLORS`(已有)
- Produces:
  - `class RoofGeometry(BaseModel)`:`kind: Literal["flat", "pitched", "hipped"]`、`ridge_y: int | None`(屋脊 y 坐标,flat 为 None)、`has_eaves: bool`
  - `class CorniceSpec(BaseModel)`:`has: bool`、`thickness: int`、`y: int | None`(檐口 y 坐标,无则 None)、`pediment: bool = False`(是否画山花三角楣,仅 neoclassic/european)
  - `class StyleConfig(ABC)` 抽象基类:`window_ratio: tuple[float, float]`、`window_arch: bool`、`def cornice(self, width_px: int) -> CorniceSpec`、`def apply_palette(self, mat: MaterialPalette) -> MaterialPalette`、`def build_roof_geom(self, params: BuildingParams) -> RoofGeometry`
  - `class FacadeSpec(BaseModel)` 升级:新增 `style: str`、`roof_geom: RoofGeometry`、`cornice: CorniceSpec`;`WindowRect` 升级新增 `arch: bool = False`(拱形窗标记);保留原字段 `roof: Literal[...]`
  - `def _roof_geom(params: BuildingParams) -> RoofGeometry` — 独立辅助,供风格子类与默认路径共用

- [ ] **Step 1: 写失败的测试(数据模型 + 兼容性)**

```python
# tests/test_facade.py 追加:
from generation.generators.simulator.facade import (
    CorniceSpec,
    RoofGeometry,
    StyleConfig,
    build_facade_spec,
)


def test_facade_spec_has_visual_fields():
    spec = build_facade_spec(_params(roof="flat"))
    # 升级后的字段存在
    assert spec.style == "modern"
    assert isinstance(spec.roof_geom, RoofGeometry)
    assert spec.roof_geom.kind == "flat"
    assert isinstance(spec.cornice, CorniceSpec)
    # 兼容:roof 字段保留
    assert spec.roof == "flat"


def test_roof_geom_kinds():
    flat = _roof_geom(_params(roof="flat"))
    pitched = _roof_geom(_params(roof="pitched"))
    hipped = _roof_geom(_params(roof="hipped"))
    assert flat.kind == "flat"
    assert flat.ridge_y is None
    assert pitched.kind == "pitched"
    assert pitched.ridge_y == 20
    assert hipped.kind == "hipped"
    assert hipped.ridge_y == 30


def test_roof_geom_unknown_raises():
    import pytest

    # 非法 roof 值应显式抛错,而非静默兜底为 hipped
    with pytest.raises(ValueError):
        _roof_geom(_params(roof="hipped").model_copy(update={"roof": "dome"}))


def test_style_config_abstract():
    # StyleConfig 是抽象基类,不能直接实例化
    import pytest

    with pytest.raises(TypeError):
        StyleConfig()
```

- [ ] **Step 2: 运行测试验证失败**

Run: `uv run pytest tests/test_facade.py -v`
Expected: FAIL,`AttributeError: 'FacadeSpec' object has no attribute 'roof_geom'`

- [ ] **Step 3: 升级 facade.py 数据模型**

在 `generation/generators/simulator/facade.py` 中:
1. 顶部 import `from abc import ABC, abstractmethod`
2. 新增 `RoofGeometry`、`CorniceSpec`(见 Interfaces)
3. `WindowRect` 新增字段 `arch: bool = False`
4. 新增 `StyleConfig(ABC)` 抽象基类(见 Interfaces)
5. `FacadeSpec` 新增 `style: str`、`roof_geom: RoofGeometry`、`cornice: CorniceSpec`,保留 `roof` 字段
6. 新增 `_roof_geom(params) -> RoofGeometry` 辅助
7. 更新 `build_facade_spec`:填充新字段(本任务先用默认 `RoofGeometry`/`CorniceSpec` 与 `style=params.style`,风格子类在 Task 3 接入)

```python
# 关键新增代码(嵌入 facade.py)
class RoofGeometry(BaseModel):
    kind: Literal["flat", "pitched", "hipped"]
    ridge_y: int | None = None
    has_eaves: bool = True


class CorniceSpec(BaseModel):
    has: bool
    thickness: int = 4
    y: int | None = None


class StyleConfig(ABC):
    window_ratio: tuple[float, float] = (1.0, 1.0)
    window_arch: bool = False

    @abstractmethod
    def cornice(self, width_px: int) -> CorniceSpec: ...

    @abstractmethod
    def apply_palette(self, mat: MaterialPalette) -> MaterialPalette: ...

    @abstractmethod
    def build_roof_geom(self, params: BuildingParams) -> RoofGeometry: ...


def _roof_geom(params: BuildingParams) -> RoofGeometry:
    """按 roof 参数生成屋顶几何。非法值显式抛错,不静默兜底。"""
    if params.roof == "flat":
        return RoofGeometry(kind="flat", ridge_y=None)
    if params.roof == "pitched":
        return RoofGeometry(kind="pitched", ridge_y=20)
    if params.roof == "hipped":
        return RoofGeometry(kind="hipped", ridge_y=30)
    raise ValueError(f"Unknown roof kind: {params.roof}")
```

- [ ] **Step 4: 运行测试验证通过**

Run: `uv run pytest tests/test_facade.py -v`
Expected: 新增 3 测试 PASS,原 7 测试保持 PASS

- [ ] **Step 5: 运行全量测试确认无回归**

Run: `uv run pytest -q`
Expected: 36 passed(33 原 + 3 新)

- [ ] **Step 6: 提交**

```bash
git add generation/generators/simulator/facade.py tests/test_facade.py
git commit -m "feat: FacadeSpec 数据模型升级(RoofGeometry/CorniceSpec/StyleConfig 基类)"
```

---

### Task 2: 屋顶几何与 renderer 屋顶绘制

**Files:**
- Modify: `generation/generators/simulator/renderer.py`
- Modify: `tests/test_renderer.py`

**Interfaces:**
- Consumes: `RoofGeometry`/`FacadeSpec`(Task 1)
- Produces:
  - `def _draw_roof(draw: ImageDraw.ImageDraw, spec: FacadeSpec, front, back) -> None` — 按 `spec.roof_geom.kind` 画屋顶(flat 平顶+檐口、pitched 山墙三角+坡面+屋脊、hipped 四坡梯形顶)
  - `_draw_facade_perspective` 改为调用 `_draw_roof`,替换硬编码平顶

- [ ] **Step 1: 写失败的测试(屋顶 PNG 差异)**

```python
# tests/test_renderer.py 追加:
def test_render_roof_kinds_produce_different_png(tmp_path: Path):
    from generation.generators.simulator.facade import build_facade_spec

    files = {}
    for roof in ("flat", "pitched", "hipped"):
        spec = build_facade_spec(_params(roof=roof))
        out = tmp_path / f"{roof}.png"
        renderer._render_facade_png(spec, out)
        files[roof] = out
        assert out.exists()
        assert out.stat().st_size > 0
    # 三种屋顶 PNG 不应完全相同(视觉差异化生效)
    flat_bytes = files["flat"].read_bytes()
    pitched_bytes = files["pitched"].read_bytes()
    hipped_bytes = files["hipped"].read_bytes()
    assert flat_bytes != pitched_bytes
    assert flat_bytes != hipped_bytes
    assert pitched_bytes != hipped_bytes
```

- [ ] **Step 2: 运行测试验证失败**

Run: `uv run pytest tests/test_renderer.py::test_render_roof_kinds_produce_different_png -v`
Expected: FAIL,当前三种屋顶渲染完全相同(PNG 字节相等)

- [ ] **Step 3: 实现 _draw_roof 并接入**

在 `generation/generators/simulator/renderer.py`:
1. 新增 `_draw_roof(draw, spec, front, back)`:按 `spec.roof_geom.kind` 分派
   - **flat**:平顶 + 顶面挑檐细线(顶面已有,加檐口描边)
   - **pitched**:前景山墙三角(以正立面顶边为底,顶点为屋脊)+ 坡面(顶面画两条斜面到屋脊)+ 屋脊线
   - **hipped**:顶面画梯形坡面(四坡)+ 屋脊线
2. `_draw_facade_perspective` 中,把现有的"顶面+侧壁"多边形逻辑改为调用 `_draw_roof`,按 roof 类型构建屋顶多边形

```python
# 关键实现(嵌入 renderer.py)
def _draw_roof(draw: ImageDraw.ImageDraw, spec: FacadeSpec, front, back) -> None:
    x0 = front[0].x
    x1 = front[1].x
    y_top = front[0].y
    roof = spec.roof_geom
    if roof.kind == "flat":
        # 平顶:顶面 + 檐口挑檐线(顶部描边)
        draw.polygon(
            [(front[0].x, front[0].y), (front[1].x, front[1].y),
             (back[1].x, back[1].y), (back[0].x, back[0].y)],
            fill=spec.palette.accent, outline=spec.palette.trim, width=3,
        )
    elif roof.kind == "pitched":
        ridge = roof.ridge_y or (y_top - 40)
        cx = (x0 + x1) / 2
        # 前景山墙三角:顶边两角 → 屋脊顶点
        draw.polygon(
            [(x0, y_top), (x1, y_top), (cx, ridge)],
            fill=spec.palette.accent, outline=spec.palette.trim, width=2,
        )
        # 坡面:顶边 → 屋脊(斜向纵深)
        back_ridge = ((back[0].x + back[1].x) / 2, ridge)
        draw.polygon(
            [(x0, y_top), (cx, ridge), back_ridge, (back[0].x, back[0].y)],
            fill=spec.palette.main, outline=spec.palette.trim, width=1,
        )
        # 屋脊线
        draw.line([cx, ridge, back_ridge[0], back_ridge[1]], fill=spec.palette.trim, width=3)
    elif roof.kind == "hipped":
        ridge = roof.ridge_y or (y_top - 30)
        cx = (x0 + x1) / 2
        back_cx = (back[0].x + back[1].x) / 2
        # 四坡:顶面梯形(前后边缩短到屋脊)
        draw.polygon(
            [(cx, ridge), (back_cx, ridge), (back[1].x, back[1].y), (x1, y_top)],
            fill=spec.palette.main, outline=spec.palette.trim, width=2,
        )
        draw.polygon(
            [(x0, y_top), (cx, ridge), (back_cx, ridge), (back[0].x, back[0].y)],
            fill=spec.palette.accent, outline=spec.palette.trim, width=1,
        )
        draw.line([cx, ridge, back_cx, ridge], fill=spec.palette.trim, width=3)
```

- [ ] **Step 4: 运行测试验证通过**

Run: `uv run pytest tests/test_renderer.py::test_render_roof_kinds_produce_different_png -v`
Expected: PASS,三种屋顶 PNG 字节不同

- [ ] **Step 5: 运行全量测试确认无回归**

Run: `uv run pytest -q`
Expected: 38 passed(37 前 + 1 新屋顶差异测试)

- [ ] **Step 6: 提交**

```bash
git add generation/generators/simulator/renderer.py tests/test_renderer.py
git commit -m "feat: 渲染器写实屋顶绘制(flat/pitched/hipped 三种形态)"
```

---

### Task 3: 风格子类(StyleConfig 具体实现 + 注册表)

**Files:**
- Modify: `generation/generators/simulator/facade.py`
- Modify: `tests/test_facade.py`

**Interfaces:**
- Consumes: `StyleConfig`/`RoofGeometry`/`CorniceSpec`/`MaterialPalette`(Task 1)
- Produces:
  - `class ModernStyle(StyleConfig)`:`window_ratio=(3.0, 1.0)`、`window_arch=False`、无檐口、中性偏冷配色
  - `class NeoclassicStyle(StyleConfig)`:`window_ratio=(0.6, 1.2)`、`window_arch=False`、厚檐口+山花、暖灰配色
  - `class EuropeanStyle(StyleConfig)`:`window_ratio=(0.8, 1.2)`、`window_arch=True`、细檐口+山花、暖调配色
  - `class NordicStyle(StyleConfig)`:`window_ratio=(1.0, 1.0)`、`window_arch=False`、木色薄檐口、低饱和配色
  - `STYLE_REGISTRY: dict[str, type[StyleConfig]]` = 4 风格映射
  - `build_facade_spec` 按 `params.style` 实例化对应子类,用其 `window_ratio`/`cornice`/`apply_palette` 生成窗型/檐口/配色;`_roof_geom` 供各子类 `build_roof_geom` 复用

- [ ] **Step 1: 写失败的测试(风格差异确定性)**

```python
# tests/test_facade.py 追加:
def test_style_window_ratio_differs():
    from generation.generators.simulator.facade import STYLE_REGISTRY

    modern = build_facade_spec(_params(style="modern"))
    neoclassic = build_facade_spec(_params(style="neoclassic"))
    # 窗宽高比不同(横长 vs 竖窗)
    m_ratio = modern.windows[0].w / modern.windows[0].h
    n_ratio = neoclassic.windows[0].w / neoclassic.windows[0].h
    assert m_ratio > n_ratio


def test_style_cornice_differs():
    modern = build_facade_spec(_params(style="modern"))
    neoclassic = build_facade_spec(_params(style="neoclassic"))
    assert modern.cornice.has is False
    assert neoclassic.cornice.has is True
    assert neoclassic.cornice.thickness > modern.cornice.thickness


def test_style_palette_differs():
    modern = build_facade_spec(_params(style="modern", materials=["stone"]))
    nordic = build_facade_spec(_params(style="nordic", materials=["stone"]))
    # 同材质不同风格 → 不同配色(风格叠加材质)
    assert modern.palette.main != nordic.palette.main


def test_style_registry_keys():
    from generation.generators.simulator.facade import STYLE_REGISTRY

    assert set(STYLE_REGISTRY.keys()) == {"modern", "neoclassic", "european", "nordic"}


def test_european_arch_window():
    european = build_facade_spec(_params(style="european"))
    assert european.windows[0].arch is True
    modern = build_facade_spec(_params(style="modern"))
    assert modern.windows[0].arch is False


def test_all_styles_windows_within_canvas():
    """回归:任一风格 × 任一层数,窗口都不超出画布。"""
    for style in ("modern", "neoclassic", "european", "nordic"):
        for floors in (1, 3, 6):
            spec = build_facade_spec(_params(style=style, floors=floors))
            for w in spec.windows:
                assert 0 <= w.x and w.x + w.w <= spec.width_px
                assert 0 <= w.y and w.y + w.h <= spec.height_px
```

- [ ] **Step 2: 运行测试验证失败**

Run: `uv run pytest tests/test_facade.py -v`
Expected: FAIL,`NameError: name 'STYLE_REGISTRY' is not defined`

- [ ] **Step 3: 实现四风格子类 + 注册表 + build_facade_spec 接入**

在 `generation/generators/simulator/facade.py`:
1. 新增 `ModernStyle`/`NeoclassicStyle`/`EuropeanStyle`/`NordicStyle` 四个子类,各自实现 `cornice`/`apply_palette`/`build_roof_geom`(build_roof_geom 复用 `_roof_geom`)
2. 新增 `STYLE_REGISTRY = {"modern": ModernStyle, "neoclassic": NeoclassicStyle, "european": EuropeanStyle, "nordic": NordicStyle}`
3. `build_facade_spec` 改为:实例化 `STYLE_REGISTRY[params.style]()`,用 `style.window_ratio` 计算窗宽高、`style.cornice(width_px)` 生成檐口、`style.apply_palette(MATERIAL_COLORS[materials[0]])` 生成配色、`style.build_roof_geom(params)` 生成屋顶几何

```python
# 关键实现(嵌入 facade.py)
class ModernStyle(StyleConfig):
    window_ratio = (3.0, 1.0)
    window_arch = False

    def cornice(self, width_px: int) -> CorniceSpec:
        return CorniceSpec(has=False, thickness=0, y=None)

    def apply_palette(self, mat: MaterialPalette) -> MaterialPalette:
        # 中性偏冷:主色去饱和,微偏蓝
        return MaterialPalette(main=_shift_hex(mat.main, -10, -10, 8), accent=mat.accent, trim=mat.trim)

    def build_roof_geom(self, params: BuildingParams) -> RoofGeometry:
        return _roof_geom(params)


class NeoclassicStyle(StyleConfig):
    window_ratio = (0.6, 1.2)
    window_arch = False

    def cornice(self, width_px: int) -> CorniceSpec:
        return CorniceSpec(has=True, thickness=8, y=20, pediment=True)

    def apply_palette(self, mat: MaterialPalette) -> MaterialPalette:
        # 暖灰:主色加灰调,降低饱和度
        return MaterialPalette(main=_shift_hex(mat.main, 8, 0, -8), accent=mat.accent, trim=mat.trim)

    def build_roof_geom(self, params: BuildingParams) -> RoofGeometry:
        return _roof_geom(params)


class EuropeanStyle(StyleConfig):
    window_ratio = (0.8, 1.2)
    window_arch = True

    def cornice(self, width_px: int) -> CorniceSpec:
        return CorniceSpec(has=True, thickness=5, y=16, pediment=True)

    def apply_palette(self, mat: MaterialPalette) -> MaterialPalette:
        # 暖调:主色偏暖
        return MaterialPalette(main=_shift_hex(mat.main, 10, 2, -12), accent=mat.accent, trim=mat.trim)

    def build_roof_geom(self, params: BuildingParams) -> RoofGeometry:
        return _roof_geom(params)


class NordicStyle(StyleConfig):
    window_ratio = (1.0, 1.0)
    window_arch = False

    def cornice(self, width_px: int) -> CorniceSpec:
        return CorniceSpec(has=True, thickness=3, y=12)

    def apply_palette(self, mat: MaterialPalette) -> MaterialPalette:
        # 低饱和:主色去饱和偏灰
        return MaterialPalette(main=_shift_hex(mat.main, -6, -6, -6), accent=mat.accent, trim=mat.trim)

    def build_roof_geom(self, params: BuildingParams) -> RoofGeometry:
        return _roof_geom(params)


def _shift_hex(hex_color: str, dr: int, dg: int, db: int) -> str:
    """将 #RRGGBB 的 RGB 分量各偏移 delta,裁剪到 [0, 255],返回新 hex。"""
    r = int(hex_color[1:3], 16) + dr
    g = int(hex_color[3:5], 16) + dg
    b = int(hex_color[5:7], 16) + db
    r = max(0, min(255, r))
    g = max(0, min(255, g))
    b = max(0, min(255, b))
    return f"#{r:02x}{g:02x}{b:02x}"


STYLE_REGISTRY: dict[str, type[StyleConfig]] = {
    "modern": ModernStyle,
    "neoclassic": NeoclassicStyle,
    "european": EuropeanStyle,
    "nordic": NordicStyle,
}
```

`build_facade_spec` 更新为使用风格子类(详见源码,保持 `build_facade_spec(params, width_px, height_px)` 签名不变):

```python
def build_facade_spec(params, width_px=640, height_px=480):
    style_cls = STYLE_REGISTRY[params.style]
    style = style_cls()
    palette = style.apply_palette(MATERIAL_COLORS[params.materials[0]])
    floors = params.floors
    margin = int(40 * (width_px / MAX_WIDTH_PX))
    ratio = style.window_ratio
    window_w = int(90 * (width_px / MAX_WIDTH_PX))
    # 窗高:风格 ratio 推导,但不超过楼层可用高(保证任意 floors 不越画布)
    ys, row_h = _window_rows(floors, height_px, margin)
    style_h = int(window_w * ratio[1] / ratio[0])
    window_h = max(20, min(style_h, row_h))
    cols = _window_cols(params.width_m)
    gap = (width_px - 2 * margin - cols * window_w) // (cols - 1) if cols > 1 else 0
    windows = []
    for y in ys:
        for c in range(cols):
            x = margin + c * (window_w + gap)
            windows.append(WindowRect(x=x, y=y, w=window_w, h=window_h, arch=style.window_arch))
    return FacadeSpec(
        width_px=width_px, height_px=height_px, floors=floors,
        windows=windows, roof=params.roof, palette=palette,
        style=params.style, roof_geom=style.build_roof_geom(params),
        cornice=style.cornice(width_px),
    )
```

- [ ] **Step 4: 运行测试验证通过**

Run: `uv run pytest tests/test_facade.py -v`
Expected: 新增 6 测试 PASS,原测试保持 PASS

- [ ] **Step 5: 运行全量测试确认无回归**

Run: `uv run pytest -q`
Expected: 43 passed

- [ ] **Step 6: 提交**

```bash
git add generation/generators/simulator/facade.py tests/test_facade.py
git commit -m "feat: 四风格子类(STYLE_REGISTRY + 窗型/檐口/配色差异化)"
```

---

### Task 4: 渲染器风格差异绘制(檐口 + 拱形窗 + 窗型)

**Files:**
- Modify: `generation/generators/simulator/renderer.py`
- Modify: `tests/test_renderer.py`

**Interfaces:**
- Consumes: `CorniceSpec`/`WindowRect.arch`(Task 1、Task 3)
- Produces:
  - `def _draw_cornice(draw, spec) -> None` — 按 `spec.cornice` 画檐口线(有檐口时)
  - `_draw_window(draw, window)` 升级 — `window.arch` 为真时画拱形窗(顶部圆弧),否则矩形
  - `_draw_facade_perspective` 调用 `_draw_cornice`,窗绘制改走升级版

- [ ] **Step 1: 写失败的测试(风格 PNG 差异)**

```python
# tests/test_renderer.py 追加:
def test_render_styles_produce_different_png(tmp_path: Path):
    from generation.generators.simulator.facade import build_facade_spec

    files = {}
    for style in ("modern", "neoclassic", "european", "nordic"):
        spec = build_facade_spec(_params(style=style))
        out = tmp_path / f"{style}.png"
        renderer._render_facade_png(spec, out)
        files[style] = out
        assert out.exists()
        assert out.stat().st_size > 0
    # 四种风格 PNG 不应完全相同
    b1 = files["modern"].read_bytes()
    assert b1 != files["neoclassic"].read_bytes()
    assert b1 != files["european"].read_bytes()
    assert b1 != files["nordic"].read_bytes()
```

- [ ] **Step 2: 运行测试验证失败**

Run: `uv run pytest tests/test_renderer.py::test_render_styles_produce_different_png -v`
Expected: FAIL,当前四风格渲染可能部分相同

- [ ] **Step 3: 实现 _draw_cornice 与拱形窗**

在 `generation/generators/simulator/renderer.py`:
1. 新增 `_draw_cornice(draw, spec)`:
   - `spec.cornice.has` 为真时,在正立面顶部画厚檐口线(`cornice.y` 位置,`thickness` 厚度,用 `palette.trim` 色)
   - 山花(neoclassic/european)在檐口上画三角楣
2. `_draw_facade_perspective` 的窗绘制逻辑升级:
   - `window.arch` 为真:矩形 + 顶部半圆弧(用 `draw.arc` 或 `draw.pieslice`)
   - 否则:矩形(现有逻辑)

```python
# 关键实现(嵌入 renderer.py)
def _draw_cornice(draw: ImageDraw.ImageDraw, spec: FacadeSpec) -> None:
    c = spec.cornice
    if not c.has:
        return
    x0, y0 = 30, 30
    x1 = spec.width_px - 30
    cy = (c.y or 0) + y0
    draw.rectangle(
        [x0, cy, x1, cy + c.thickness],
        fill=spec.palette.trim, outline=spec.palette.trim,
    )
    # 山花三角楣:仅 pediment=True 的风格(neoclassic/european)画
    if c.pediment:
        pediment_h = max(10, c.thickness * 2)
        draw.polygon(
            [(x0, cy), (x1, cy), ((x0 + x1) / 2, cy - pediment_h)],
            outline=spec.palette.trim, fill=spec.palette.accent,
        )
```

拱形窗在窗绘制处(`window.arch` 为真时,窗顶用 `draw.pieslice` 画半圆拱)。

- [ ] **Step 4: 运行测试验证通过**

Run: `uv run pytest tests/test_renderer.py::test_render_styles_produce_different_png -v`
Expected: PASS,四风格 PNG 字节不同

- [ ] **Step 5: 运行全量测试确认无回归**

Run: `uv run pytest -q`
Expected: 43 passed

- [ ] **Step 6: 提交**

```bash
git add generation/generators/simulator/renderer.py tests/test_renderer.py
git commit -m "feat: 渲染器檐口/山花/拱形窗绘制(风格差异可见)"
```

---

### Task 5: 端到端冒烟验证(阶段 2.5 完成标准)

**Files:**
- Modify: `docs/gallery/smoke-test.md`(追加阶段 2.5 记录)

**Interfaces:**
- Consumes: Task 1-4 全部产物

- [ ] **Step 1: 运行全量测试与 lint**

Run:
```bash
uv run pytest -q
uv run ruff check . && uv run ruff format --check .
```
Expected: 全部 PASS,ruff 无报错。

- [ ] **Step 2: 启动后端并验证视觉差异化**

Run(终端 A): `uv run archgen-api`
Expected: uvicorn 在 8000 端口启动。

Run(终端 B):
```bash
# 生成四种风格 + 三种屋顶的 facade PNG,确认 URL 可访问
curl -s -X POST http://localhost:8000/api/v1/generate -H "Content-Type: application/json" \
  -d '{"params":{"style":"modern","floors":3,"width_m":10,"depth_m":8,"materials":["glass"],"roof":"flat","environment":"suburb"},"lang":"zh"}'
```
Expected: 返回 images 数组含 facade/floorplan URL,HTTP 200。

- [ ] **Step 3: 目检(可选)**

将四种风格 facade.png 存到 `docs/gallery/`,人工确认视觉差异(窗型/檐口/屋顶明显不同)。

- [ ] **Step 4: 记录冒烟结果**

```bash
echo "- 阶段2.5: 三种屋顶(flat/pitched/hipped)渲染差异化" >> docs/gallery/smoke-test.md
echo "- 阶段2.5: 四风格(modern/neoclassic/european/nordic)窗型/檐口/配色差异化" >> docs/gallery/smoke-test.md
echo "- 阶段2.5: 全量测试通过(43),ruff 全绿" >> docs/gallery/smoke-test.md
```

- [ ] **Step 5: 提交**

```bash
git add docs/gallery/smoke-test.md
git commit -m "docs: 记录阶段 2.5 冒烟验证结果"
```

---

## Self-Review

**Spec coverage(对照阶段 2.5 设计规格):**
- ✅ FacadeSpec 数据模型升级(RoofGeometry/CorniceSpec/StyleConfig 基类)→ Task 1
- ✅ 写实屋顶几何与 renderer 绘制(flat/pitched/hipped)→ Task 2
- ✅ 四风格子类 + STYLE_REGISTRY(窗型/檐口/配色)→ Task 3
- ✅ 渲染器檐口/山花/拱形窗绘制 → Task 4
- ✅ 端到端冒烟验证 → Task 5

**Placeholder scan:** 无 TBD/TODO。所有代码块含完整实现。Task 3 的 `apply_palette` 用 `_shift_hex` 对主色做确定性色偏(modern 偏冷、neoclassic 暖灰、european 暖调、nordic 低饱和),保证同材质不同风格 `palette.main` 必然不同,`test_style_palette_differs` 确定性通过。

**Type consistency:**
- `build_facade_spec(params, width_px, height_px)` 签名不变,兼容现有调用
- `FacadeSpec` 保留 `roof` 字段(测试 `test_roof_style_variation` 断言 `flat.roof == "flat"`)
- `WindowRect.arch` 默认 `False`,兼容 Task 2 已建窗
- `StyleConfig` 抽象方法 `cornice`/`apply_palette`/`build_roof_geom` 由四子类一致实现

**确定性说明:** 所有绘图由 `BuildingParams` 唯一决定(无随机),测试可复现。屋顶 ridge_y/檐口 y 为固定值或按 params 推导。

**兼容性说明:** Task 3 后 `build_facade_spec` 的窗宽高由 `style.window_ratio` 推导。原 `test_window_cols_by_building_width`/`test_windows_within_canvas_floors6` 需保持通过——若窗高超出画布,`_window_rows` 的均分逻辑已保证;若窗宽因 ratio 变化导致越界,需在实施时校验 gap 计算,确保窗不越画布。
